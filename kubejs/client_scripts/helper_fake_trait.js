global.addFakeTrait = function(targetItem, traitTitle, traitDescription) {
    // Adds a Fake tooltip trait to a given item, or class of item.
	
	
	// Iterate over the tooltip lines to find the line that starts with "TRAITS:"
	
	// Creates a trait line if none already existed
	
	// insert the "fake trait" after the traits line.
};